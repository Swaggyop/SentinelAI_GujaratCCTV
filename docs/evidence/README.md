# Submission evidence (A06)

Drop `npm audit` and `pip-audit` output here before the judged run. DB/DevOps pinned MinIO image tags in `docker-compose.yml`; application lockfiles come from the other packages.
