# Placeholder — Backend AI (Package A)

Read `docs/sentinel-handoff-spec.md` sections 1, 2, 3, 6, 7 and `db/README.md`.
Do not change `db/` or `docs/`.
Use `DATABASE_URL_APP` and env names from `.env.example`.
